function y = abdullah(x)
%Function command is used to indicate a function. The first line of the
%function file has to contain the keyword function.
y = x.^2 + exp(x);
% A polynomial that has independent variable x and dependent variable y,
% which uses the a default MATLAB function of exp.